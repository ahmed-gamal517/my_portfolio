import 'package:flutter/material.dart';

class HomeScreenTabletBody extends StatelessWidget {
  const HomeScreenTabletBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tablet'));
  }
}
